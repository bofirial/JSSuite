/*******************************************
 * Jumping Salamander Disabled If Module
 *******************************************/

(function (window, JS, $) {
    if (JS) {
        function DisabledIf() {

            var self = this;

            this.initializeRegion = function ($region) {

                $('.disabledIf', $region).bind('change', function (e) {

                });
            };
        }

        JS.registerModule("controls.disabledIf", DisabledIf);
    }
    else {
        throw "JS Core Module Missing.  Unable to register the DisabledIf Module";
    }

})(window, JS, $);